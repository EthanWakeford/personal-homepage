---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero';
import AboutCard from '../components/AboutCard';
import TextImagecard from '../components/TextImageCard';
import { getData } from '../firebase';

export type Project = {
  title: string;
  project_type: 'project' | 'website';
  description: string;
  url_slug: string;
  hero_picture: string;
  tools: string;
  dates: string;
  project_url: string;
  source_url: string;
};

const projectData: Project = await getData();

// const about = myData[0].about;
// const bgImage = myData[0].bgimage;
// const headShot = myData[0].headshot;
---

<Layout title='' metaDescription='this is my first astro page'>
  <Hero
    welcomeText='Ethan Wakeford'
    tagline={about}
    backgroundImages={[bgImage]}
    slideTime={5000}
    client:load
  />
  <AboutCard
    aboutTitle='this is where I make a title about some stuff that we do'
    aboutBody='its super cool'
    linkText='Click here to find out'
  />
  <TextImagecard
    title='title'
    bodyText='blah blah'
    imageAltText='cool picture'
    imageLink={headShot}
    link='/'
    linkText='click me'
    reversed={false}
  />
</Layout>
